{
  "name": "claw-knowledge-base",
  "private": true,
  "version": "1.0.0",
  "description": "Personal knowledge base with Slidev presentations",
  "scripts": {
    "dev": "npx slidev --open",
    "build": "npx slidev build",
    "export": "npx slidev export",
    "deploy": "npx slidev build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@slidev/cli": "^52.11.5",
    "gh-pages": "^6.1.1"
  }
}