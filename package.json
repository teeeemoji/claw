{
  "name": "claw-knowledge-base",
  "private": true,
  "version": "1.0.0",
  "description": "Personal knowledge base with Slidev presentations",
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export": "slidev export",
    "build:pages": "slidev build --base /claw/",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.23",
    "@slidev/theme-default": "^0.49.23",
    "gh-pages": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}